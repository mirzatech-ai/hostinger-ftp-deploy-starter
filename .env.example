# Copy this to .env and fill values (do NOT commit .env to git).
# Protocol: "sftp" (recommended) or "ftp"
FTP_PROTOCOL=sftp

# Host, user, password and remote dir
FTP_HOST=your-hostname-or-ip
FTP_PORT=22
FTP_USER=your-ftp-username
FTP_PASSWORD=your-ftp-password

# The remote folder where files should be placed.
# On Hostinger shared/Cloud this is the public_html folder for the domain.
# e.g. /home/username/domains/example.com/public_html
FTP_REMOTE_DIR=/home/username/domains/example.com/public_html

# Optional: set to "true" to delete remote files that are not present in dist/
DELETE_REMOTE=false
